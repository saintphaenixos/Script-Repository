#this is a compiled crystal program to search for text or strings of text with color output, possibly with regular expressions in the future.

content = Dir.current

puts "#{content}"

blorg = Dir.current
def entries : Array(String)
end
